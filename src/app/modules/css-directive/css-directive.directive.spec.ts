import { CssDirectiveDirective } from './css-directive.directive';

describe('CssDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CssDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
